name: 05 2 filters activity types

on:
    pull_request:
        types: [closed]
        branches: [main]
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the trigger
              run: echo "I was triggered by a ${{ github.event_name }} closed event."